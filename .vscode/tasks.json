{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build project",
            "type": "shell",
            "command": "gcc",
            "args": [
                "-std=c99", "-g", "hello.c", "-o", "hello"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
            
        }
    ]
}
